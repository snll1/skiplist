#include <cassert>
#include <climits>
#include <iostream>
#include <thread>

#include "locked_skiplist.h"
#include "skiplist.h"

using namespace skiplist;
using namespace std;

int main() {
    srand(static_cast<unsigned int>(time(nullptr)));

    return 0;
}